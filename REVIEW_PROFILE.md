# Research Reading Profile

这份文件定义 Radar 的默认阅读方式。目标不是复述作者叙事，而是从“如何为我们的研究所用”的角度完成第一轮审稿式预读。

## 核心原则

1. 先还原任务、输入输出、模型、数据、训练与推理预算，再讨论创新。
2. 把作者命名拆回真实增量：新增模块、数据管线、损失、prompt、采样、额外计算或评测控制。
3. 任何结果都检查模型规模、训练数据、rollout 数量、token/frame 预算和 benchmark 设置是否匹配。
4. 区分算法探索、数据工程、系统优化、benchmark 与 scale recipe；不因包装复杂就默认算法新颖。
5. 主动追溯最近邻方法和组件来源，说明“旧组件”“新组合”“真正新增变量”。
6. Benchmark 必须解释考察能力、样本规模、评分方式和结论边界，不能只罗列分数。
7. 所有内容区分三类：`FACT` 来自论文或代码，`INFERENCE` 是我们的判断，`IDEA` 是待验证的可复用假设。
8. 论文声称开放代码、数据或模型时必须实际访问验证；不可用时明确标红。
9. 没有 insight 时宁可不生成 Idea Candidate，不把作者 future work 改写成我们的想法。

## 每日阅读预算

- 摘要级扫描：最多 60 篇候选。
- Radar 展示：5–8 篇高相关候选。
- 主深读：默认 1 篇，必须读取正文、关键实验表和附录。
- 对照阅读：最多 2 篇，用于方法溯源或矛盾结论比较。
- Idea Candidate：每天最多 2 个。

## 深读必须回答

- 面向什么具体问题？属于推理、后训练、agent、评测还是系统？
- 输入模态、数据类型与输出是什么？
- 在哪些模型、多少参数、哪些数据、多少样本上完成？
- 是否训练？训练和推理额外成本是什么？
- 去掉作者术语后，方法到底增加了什么？
- 是否提出新数据集、benchmark、代码、模型或评测协议？是否真实可用？
- Benchmark 分别测什么能力，主要结果和公平性如何？
- 哪些思想来自已有工作？新意是组件、组合还是控制变量？
- 更偏算法还是工程？最 solid 与最 oversold 的地方分别是什么？
- 哪个变量可被迁移、替换、组合或反转，形成低成本新实验？

## 展示规则

- `abstract-only` 只能进入 `Deep-read Candidate` 或 `Watch`，不能表现成已经背书的结论。
- 站内深读页优先展示“本质上做了什么”“为什么对我们有用”“证据和风险”。
- arXiv、PDF 和代码是独立外链，不替代站内研究解读。
